---
description: Core architecture rules for the IS Textbook Platform
globs:
alwaysApply: true
---

# Architecture Rules

- Application code lives in GitHub. Book content lives in `books/` (Markdown). Firestore is the runtime database.
- NEVER store textbook chapters as HTML or Firestore JSON in Git.
- Canonical format is ISBX: Markdown + YAML frontmatter + fenced blocks in `books/`.
- Design for multiple textbooks — never assume a single book.
- Use feature folders under `src/features/`.
- Follow `docs/ARCHITECTURE.md` and `docs/DATABASE.md` for schema decisions.
- When architecture is unclear, STOP and ask for clarification. Do not invent contradictory architecture.
